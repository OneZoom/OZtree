This folder contains stuff that needs to be tailored on a per-site basis, such as the analytics code and (possibly) the options file. Other sites may wish to store their own versions of code in here.

At the moment the google-analytics file is deliberately empty.